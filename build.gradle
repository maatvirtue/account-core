subprojects {
	apply plugin: 'java'
	apply plugin: 'maven'

	defaultTasks 'tasks'

	group = 'net.nlacombe'
	version = '4.0.0'

	sourceCompatibility = 10
	targetCompatibility = 10

	repositories {
		mavenCentral()
		mavenLocal()
	}
}

wrapper {
	gradleVersion = '4.9'
}
