subprojects {
	group = 'net.nlacombe'
	version = '6.0.0'

    ext.depVersions = [
        wsUtils: '6.0.0'
    ]
    ext.javaVersion = JavaVersion.VERSION_11

	repositories {
		mavenCentral()
		mavenLocal()
	}
}

wrapper {
	gradleVersion = '6.8.3'
}
