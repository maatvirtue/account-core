subprojects {
	apply plugin: 'maven'

	defaultTasks 'tasks'

	group = 'net.nlacombe'
	version = '4.0.1'

	project.ext.javaVersion = JavaVersion.VERSION_11

	repositories {
		mavenCentral()
		mavenLocal()
	}
}

wrapper {
	gradleVersion = '6.0.1'
}
