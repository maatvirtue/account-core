subprojects {
	apply plugin: 'java'
	apply plugin: 'maven'

	defaultTasks 'tasks'

	group = 'net.nlacombe'
	version = '1.0.11'

	sourceCompatibility = 1.8
	targetCompatibility = 1.8

	repositories {
		mavenCentral()
		mavenLocal()
	}
}

task wrapper(type: Wrapper) {
	gradleVersion = '3.4.2'
}
