subprojects {
	apply plugin: 'maven'

	defaultTasks 'tasks'

	group = 'net.nlacombe'
	version = '4.1.0'

	project.ext.javaVersion = JavaVersion.VERSION_11

	repositories {
		mavenCentral()
		mavenLocal()
	}
}

wrapper {
	gradleVersion = '6.5.1'
}
